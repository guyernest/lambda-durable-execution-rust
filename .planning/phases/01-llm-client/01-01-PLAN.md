---
phase: 01-llm-client
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/Cargo.toml
  - examples/src/bin/mcp_agent/main.rs
  - examples/src/bin/mcp_agent/llm/mod.rs
  - examples/src/bin/mcp_agent/llm/models.rs
  - examples/src/bin/mcp_agent/llm/error.rs
  - examples/src/bin/mcp_agent/llm/transformers/utils.rs
autonomous: true
requirements: [LLM-01, LLM-04, LLM-06]
must_haves:
  truths:
    - "LLMInvocation, LLMResponse, ProviderConfig, FunctionCall, and all content types compile with Serialize + Deserialize"
    - "LlmError has is_retryable() method that returns true for 429/500/502/503/529 and false for 400/401/403"
    - "cargo test --manifest-path examples/Cargo.toml --all-targets passes with the new mcp_agent binary"
  artifacts:
    - path: "examples/src/bin/mcp_agent/llm/models.rs"
      provides: "Provider-agnostic LLM types (LLMInvocation, LLMResponse, ProviderConfig, UnifiedMessage, ContentBlock, FunctionCall, etc.)"
      contains: "pub struct LLMResponse"
    - path: "examples/src/bin/mcp_agent/llm/error.rs"
      provides: "LlmError enum with is_retryable() classification"
      contains: "fn is_retryable"
    - path: "examples/src/bin/mcp_agent/llm/transformers/utils.rs"
      provides: "JSON extraction helpers (safe_extract_field, clean_tool_schema)"
      contains: "pub fn clean_tool_schema"
    - path: "examples/Cargo.toml"
      provides: "Updated dependencies for mcp_agent binary"
      contains: "name = \"mcp_agent\""
  key_links:
    - from: "examples/src/bin/mcp_agent/llm/mod.rs"
      to: "models.rs, error.rs, transformers/"
      via: "pub mod declarations and re-exports"
      pattern: "pub mod models"
    - from: "examples/src/bin/mcp_agent/llm/models.rs"
      to: "serde"
      via: "derive macros"
      pattern: "Serialize, Deserialize"
---

<objective>
Create the foundation types, error handling, and project scaffold for the LLM client module.

Purpose: Establish all shared types (models, errors, utils) that transformers and service will build on. Without these, nothing else compiles. Also sets up the mcp_agent binary in Cargo.toml and creates the module structure.

Output: Compilable mcp_agent binary with complete type definitions, error classification, and JSON utility helpers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-llm-client/01-CONTEXT.md
@.planning/phases/01-llm-client/01-RESEARCH.md

@examples/Cargo.toml
@examples/src/bin/hello_world/main.rs

<interfaces>
<!-- Source types being extracted. Executor MUST read these files for the actual code. -->

From ~/projects/step-functions-agent/lambda/call_llm_rust/src/models.rs:
- LLMInvocation { provider_config, messages, tools, temperature, max_tokens, top_p, stream }
- ProviderConfig { provider_id, model_id, endpoint, auth_header_name, auth_header_prefix, secret_path, secret_key_name, request_transformer, response_transformer, timeout, custom_headers }
- UnifiedMessage { role, content: MessageContent }
- MessageContent::Text { content } | MessageContent::Blocks { content: Vec<ContentBlock> }
- ContentBlock::Text | ContentBlock::ToolUse | ContentBlock::ToolResult | ContentBlock::Image
- LLMResponse { message: AssistantMessage, function_calls: Option<Vec<FunctionCall>>, metadata: ResponseMetadata }
- AssistantMessage { role, content: Vec<ContentBlock>, tool_calls: Option<Value> }
- FunctionCall { id, name, input: Value }
- ResponseMetadata { model_id, provider_id, latency_ms, tokens_used, stop_reason }
- TokenUsage { input_tokens, output_tokens, total_tokens }
- TransformedRequest { body: Value, headers: HashMap<String, String> }
- TransformedResponse { message: AssistantMessage, function_calls, usage, stop_reason }

From ~/projects/step-functions-agent/lambda/call_llm_rust/src/error.rs:
- ServiceError with variants: TransformerNotFound, SecretNotFound, SecretKeyNotFound, AwsSdkError, HttpError, JsonError, ProviderApiError, InvalidConfiguration, TransformError, Timeout, RateLimitExceeded, LambdaError, Unknown

From ~/projects/step-functions-agent/lambda/call_llm_rust/src/transformers/utils.rs:
- safe_extract_field(value, path) -> Option<Value>
- extract_with_fallback(value, paths) -> Option<Value>
- extract_string(value, paths) -> Option<String>
- extract_u32(value, paths, default) -> u32
- generate_tool_id() -> String
- clean_tool_schema(schema) -> Value
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create mcp_agent binary scaffold, Cargo.toml updates, and foundation types (models.rs, error.rs, utils.rs)</name>
  <files>
    examples/Cargo.toml
    examples/src/bin/mcp_agent/main.rs
    examples/src/bin/mcp_agent/llm/mod.rs
    examples/src/bin/mcp_agent/llm/models.rs
    examples/src/bin/mcp_agent/llm/error.rs
    examples/src/bin/mcp_agent/llm/transformers/mod.rs
    examples/src/bin/mcp_agent/llm/transformers/utils.rs
  </files>
  <read_first>
    ~/projects/step-functions-agent/lambda/call_llm_rust/src/models.rs
    ~/projects/step-functions-agent/lambda/call_llm_rust/src/error.rs
    ~/projects/step-functions-agent/lambda/call_llm_rust/src/transformers/utils.rs
    examples/Cargo.toml
    examples/src/bin/hello_world/main.rs
  </read_first>
  <behavior>
    - Test: LLMResponse round-trips through serde_json (serialize then deserialize produces equal struct)
    - Test: ContentBlock::ToolUse serializes with "type": "tool_use" tag
    - Test: ContentBlock::ToolResult serializes with "type": "tool_result" tag and includes is_error field
    - Test: LlmError::ProviderApiError with status 429 returns is_retryable() == true
    - Test: LlmError::ProviderApiError with status 400 returns is_retryable() == false
    - Test: LlmError::ProviderApiError with status 529 returns is_retryable() == true
    - Test: LlmError::HttpError returns is_retryable() == true
    - Test: clean_tool_schema normalizes non-object input to { type: "object", properties: {} }
    - Test: safe_extract_field with dot-path "choices.0.message" extracts nested value
  </behavior>
  <action>
1. **Update examples/Cargo.toml:**
   - Add new dependencies (per D-01, using research-verified versions):
     ```toml
     reqwest = { version = "0.13", features = ["json"] }
     aws-config = { version = "1.8", features = ["behavior-version-latest"] }
     aws-sdk-secretsmanager = "1.103"
     thiserror = "2.0"
     ```
   - Add the new binary section:
     ```toml
     [[bin]]
     name = "mcp_agent"
     path = "src/bin/mcp_agent/main.rs"
     ```

2. **Create `examples/src/bin/mcp_agent/main.rs`:**
   - Minimal stub that compiles. Declare `mod llm;` at the top. The main function can be a simple placeholder Lambda handler (similar to hello_world pattern) that just returns Ok. This is a Phase 3 concern -- for now just needs to compile.
   - Use the standard entry point pattern:
     ```rust
     mod llm;

     #[tokio::main]
     async fn main() -> Result<(), lambda_runtime::Error> {
         let _ = tracing_subscriber::fmt()
             .with_env_filter(tracing_subscriber::EnvFilter::from_default_env())
             .try_init();
         // Handler will be wired in Phase 3
         Ok(())
     }
     ```

3. **Create `examples/src/bin/mcp_agent/llm/mod.rs`:**
   - Declare submodules: `pub mod models;`, `pub mod error;`, `pub mod transformers;`
   - Re-export key types: `pub use models::{LLMInvocation, LLMResponse, ProviderConfig, UnifiedMessage, ContentBlock, FunctionCall, ResponseMetadata, TokenUsage, UnifiedTool, AssistantMessage};`
   - Re-export: `pub use error::LlmError;`

4. **Create `examples/src/bin/mcp_agent/llm/models.rs`:**
   - Copy all types from `~/projects/step-functions-agent/lambda/call_llm_rust/src/models.rs`
   - CRITICAL ADAPTATION: Add `Deserialize` to ALL response types that only have `Serialize`:
     - `LLMResponse` -> `#[derive(Debug, Clone, Serialize, Deserialize)]`
     - `AssistantMessage` -> `#[derive(Debug, Clone, Serialize, Deserialize)]`
     - `FunctionCall` -> `#[derive(Debug, Clone, Serialize, Deserialize)]`
     - `ResponseMetadata` -> `#[derive(Debug, Clone, Serialize, Deserialize)]`
     - `TokenUsage` -> `#[derive(Debug, Clone, Serialize, Deserialize)]`
   - Add `Serialize` to request types that only have `Deserialize` (for logging):
     - `LLMInvocation` -> `#[derive(Debug, Clone, Serialize, Deserialize)]`
     - `ProviderConfig` -> `#[derive(Debug, Clone, Serialize, Deserialize)]`
   - Add `is_error: Option<bool>` field to `ContentBlock::ToolResult` variant (per research -- needed for MCP error propagation in Phase 3). The existing code has `ToolResult { tool_use_id, content }` -- change to `ToolResult { tool_use_id, content, #[serde(skip_serializing_if = "Option::is_none")] is_error: Option<bool> }`
   - Keep `TransformedRequest` and `TransformedResponse` as internal (non-pub-from-mod) types
   - Use `serde_json::Value` for the same places the original does (tool input, tool_calls, schemas)
   - Keep the `default_timeout` function returning 30

5. **Create `examples/src/bin/mcp_agent/llm/error.rs`:**
   - Rename `ServiceError` to `LlmError` (per Claude's Discretion on error type design)
   - Remove the `LambdaError` variant (not needed -- agent uses DurableError at handler boundary)
   - Remove `RateLimitExceeded` variant (429 is handled via ProviderApiError + is_retryable)
   - Keep these variants: `TransformerNotFound(String)`, `SecretNotFound(String)`, `SecretKeyNotFound(String, String)`, `HttpError(#[from] reqwest::Error)`, `JsonError(#[from] serde_json::Error)`, `ProviderApiError { provider: String, status: u16, message: String }`, `InvalidConfiguration(String)`, `TransformError(String)`, `Timeout(u64)`, `Unknown(String)`
   - For AwsSdkError, use `#[error("AWS SDK error: {0}")] AwsSdkError(String)` instead of `#[from] aws_sdk_secretsmanager::Error` -- store as String to avoid tight coupling to SDK error types (per Research Pitfall 6). The From impl will convert via `.to_string()`.
   - Add `is_retryable(&self) -> bool` method:
     ```rust
     pub fn is_retryable(&self) -> bool {
         match self {
             LlmError::ProviderApiError { status, .. } => matches!(
                 status, 429 | 500 | 502 | 503 | 529
             ),
             LlmError::HttpError(_) => true,
             LlmError::Timeout(_) => true,
             _ => false,
         }
     }
     ```
   - Implement `From<LlmError> for Box<dyn std::error::Error + Send + Sync>` so it works with `ctx.step()`:
     ```rust
     impl From<LlmError> for Box<dyn std::error::Error + Send + Sync> {
         fn from(err: LlmError) -> Self {
             Box::new(err)
         }
     }
     ```

6. **Create `examples/src/bin/mcp_agent/llm/transformers/mod.rs`:**
   - For now, just declare submodules that will exist: `pub mod utils;`
   - The `MessageTransformer` trait and `TransformerRegistry` will be added in Plan 02. Add a placeholder comment: `// MessageTransformer trait and TransformerRegistry added in plan 02`

7. **Create `examples/src/bin/mcp_agent/llm/transformers/utils.rs`:**
   - Copy all functions from `~/projects/step-functions-agent/lambda/call_llm_rust/src/transformers/utils.rs`
   - Keep all 6 functions: `safe_extract_field`, `extract_with_fallback`, `extract_string`, `extract_u32`, `generate_tool_id`, `clean_tool_schema`
   - Remove the `use tracing::warn;` since none of these functions use tracing (verify in source)
   - All functions stay exactly as they are -- no adaptations needed

8. **Write tests in a `#[cfg(test)] mod tests` block at the bottom of each file:**
   - In `models.rs`: test serde round-trip for LLMResponse, test ContentBlock serialization tags
   - In `error.rs`: test is_retryable for each relevant status code
   - In `transformers/utils.rs`: test safe_extract_field with nested paths, test clean_tool_schema normalization
  </action>
  <verify>
    <automated>cd /Users/guy/Development/mcp/lambda-durable-execution-rust && cargo test --manifest-path examples/Cargo.toml --all-targets 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - examples/Cargo.toml contains `name = "mcp_agent"` in a [[bin]] section
    - examples/Cargo.toml contains `reqwest = { version = "0.13", features = ["json"] }`
    - examples/Cargo.toml contains `aws-sdk-secretsmanager = "1.103"`
    - examples/src/bin/mcp_agent/main.rs contains `mod llm;`
    - examples/src/bin/mcp_agent/llm/models.rs contains `#[derive(Debug, Clone, Serialize, Deserialize)]` before `pub struct LLMResponse`
    - examples/src/bin/mcp_agent/llm/models.rs contains `pub struct ProviderConfig`
    - examples/src/bin/mcp_agent/llm/models.rs contains `pub struct FunctionCall`
    - examples/src/bin/mcp_agent/llm/models.rs contains `is_error: Option<bool>` inside ToolResult variant
    - examples/src/bin/mcp_agent/llm/error.rs contains `pub fn is_retryable(&self) -> bool`
    - examples/src/bin/mcp_agent/llm/error.rs contains `429 | 500 | 502 | 503 | 529`
    - examples/src/bin/mcp_agent/llm/error.rs does NOT contain `LambdaError`
    - examples/src/bin/mcp_agent/llm/transformers/utils.rs contains `pub fn clean_tool_schema`
    - examples/src/bin/mcp_agent/llm/transformers/utils.rs contains `pub fn safe_extract_field`
    - `cargo test --manifest-path examples/Cargo.toml --all-targets` exits 0
    - `cargo clippy --manifest-path examples/Cargo.toml --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    All foundation types compile with Serialize + Deserialize. LlmError has working is_retryable() classification. JSON utils are functional. mcp_agent binary compiles. All unit tests pass.
  </done>
</task>

</tasks>

<verification>
- `cargo test --manifest-path examples/Cargo.toml --all-targets` passes (all existing + new tests)
- `cargo clippy --manifest-path examples/Cargo.toml --all-targets -- -D warnings` passes
- `cargo fmt --manifest-path examples/Cargo.toml --check` passes
- Types round-trip through serde_json correctly
</verification>

<success_criteria>
- mcp_agent binary entry in Cargo.toml and minimal main.rs that compiles
- All LLM types (LLMInvocation, LLMResponse, ProviderConfig, FunctionCall, etc.) defined with Serialize + Deserialize
- LlmError enum with is_retryable() returning correct classification for all status codes
- JSON utility helpers (safe_extract_field, clean_tool_schema, etc.) working with tests
- ToolResult has is_error: Option<bool> field for future MCP error propagation
- All tests pass, clippy clean, fmt clean
</success_criteria>

<output>
After completion, create `.planning/phases/01-llm-client/01-01-SUMMARY.md`
</output>
